# Task Plan

- [x] Finish async multi-environment gateway routing for submit, poll, and metrics across `mathlib-v4.15`, `mathlib-v4.27`, and `formal-conjectures-v4.27`. Proof: `PYTHONPATH=./client ./.venv/bin/pytest tests/test_environment_gateway.py tests/test_async_router.py tests/test_client_environment.py tests/test_request_policy.py tests/test_sync_api_unchanged.py tests/test_auth_hardening.py -q` -> `44 passed in 1.65s`.
- [x] Pin FormalConjectures to an exact commit and wire the Railway helper/build config to use Lean `v4.27.0` with the project’s real dependency graph. Proof: `backend/scripts/railway_configure_multi_env.py` now pins `LEAN_PROJECT_REF=c18e2336abba12b96b75c0ea4a894342a64037bb`; dry-run artifact written to `/Users/akhildeo/Desktop/Johns-Hopkins/Reasoning/lean-ui/backend/outputs/loadtests/verification/railway_multi_env_plan_1773276212.json`.
- [x] Harden Railway multi-environment topology for private checker routing, service creation planning, deploy validation, and documented capacity defaults. Proof: `PYTHONPATH=./client ./.venv/bin/python scripts/railway_configure_multi_env.py` completed successfully in dry-run mode and produced a plan with internal checker URLs plus `services_to_create=["lean-ui-mathlib427","lean-ui-formalconjectures427"]`.
- [x] Fix the frontend build blocker so `npm run build` passes in a clean environment and add a repeatable build check. Proof: `npm run build` passed on 2026-03-12 after `scripts/ensure-lightningcss.js` repaired the missing native package; CI workflow added at `/Users/akhildeo/Desktop/Johns-Hopkins/Reasoning/lean-ui/.github/workflows/frontend-build.yml`.
- [ ] Push/deploy the current code to GitHub/Railway, create the two checker services in Railway, and rerun authenticated production load tests with performance gating on. Blocker: the repo changes are local only; the Railway helper can now create/configure `lean-ui-mathlib427` and `lean-ui-formalconjectures427`, but executing it before the current worktree is pushed would deploy old code from remote `main`, not this implementation.
