# Task Plan

- [x] Revert the pushed multi-environment Lean rollout commits while preserving the separate async-throughput Railway tuning edits. Proof: pushed rollback commits `69f560b` (`Revert "Add multi-environment Lean gateway rollout"`) and `dcee987` (`Preserve async queue compatibility after rollback`).
- [x] Verify the repo is back to single-environment Lean `v4.15.0` behavior with backend tests and a frontend production build. Proof: `npm run build` passed on 2026-03-11; `PYTHONPATH=./client ./.venv/bin/pytest tests/test_async_queue.py tests/test_request_policy.py tests/test_header_offset.py -q` -> `11 passed in 0.24s`. Note: local REPL-backed tests are blocked on this macOS host by a dyld loader failure in `backend/repl/.lake/build/bin/repl`, so live Railway smoke tests were used for end-to-end Lean verification.
- [x] Roll Railway back to one public `lean-ui` Lean `v4.15.0` service, keep `lean-ui-worker`/`lean-ui-redis`, and delete the extra 4.27 checker services. Proof: Railway service list now contains only `lean-ui`, `lean-ui-worker`, and `lean-ui-redis`; `lean-ui` and `lean-ui-worker` both reached `SUCCESS` on commit `dcee9876f56829f6a8cdcb0e9f6ef76f1d152023`; `LEAN_SERVER_GATEWAY_ENVIRONMENTS`, `LEAN_SERVER_GATEWAY_DEFAULT_ENVIRONMENT`, and `LEAN_SERVER_GATEWAY_INTERNAL_API_KEY` were deleted from `lean-ui`.
- [x] Validate production with authenticated sync and async smoke checks plus an async-heavy load test. Proof: `GET https://lean-ui-production.up.railway.app/health` -> `200 {"status":"ok"}`; authenticated `POST /api/check` with `#check Nat` -> `200`; authenticated `GET /api/environments` -> `404`; authenticated async submit/poll and `GET /api/async/metrics` succeeded; async benchmark artifact written to `/Users/akhildeo/Desktop/Johns-Hopkins/Reasoning/lean-ui/backend/outputs/loadtests/verification/rollback_async_loadtest_summary.json` with `16/16` completions, `0` submit errors, and `0` poll timeouts.
