# Task Plan

- [x] Increase Railway async worker replicas from `3` to `6` in the checked-in ops defaults and in production. Proof: `backend/scripts/check_railway_state.py`, `backend/scripts/railway_apply_async_rollout.py`, and `backend/scripts/railway_tune_prod_capacity.py` now default to `6`; `PYTHONPATH=./client ./backend/.venv/bin/python backend/scripts/railway_tune_prod_capacity.py` dry-run reported `"worker_replicas": 6`; Railway worker manifest now reports `multiRegionConfig.us-east4-eqdc4a.numReplicas = 6`; public health and authenticated `POST /api/check` both returned `200` after the live change.
