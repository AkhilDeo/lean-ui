# Task Plan

- [x] Lock the verifier back to one Mathlib Lean runtime and remove alternate-environment build/runtime assumptions. Proof: `backend/Dockerfile` now exports the intended single-runtime REPL/mathlib build args into `setup.sh`; `backend/setup.sh` now defaults to `https://github.com/FrederickPu/repl.git@lean415compat` plus `mathlib4@v4.15.0`; `backend/tests/test_infra_config_scripts.py` asserts those defaults.
- [x] Surface truthful REPL startup/header failures instead of generic JSON decode / header errors. Proof: `backend/server/repl.py` now captures subprocess stderr/exit code and reports `REPL returned empty stdout (...)`; `backend/server/manager.py` propagates detailed startup/header failures; `PYTHONPATH=./backend/client ./backend/.venv/bin/pytest backend/tests/test_repl.py backend/tests/test_infra_config_scripts.py backend/tests/test_async_queue.py backend/tests/test_async_jobs.py backend/tests/test_async_router.py backend/tests/test_async_worker.py backend/tests/test_sync_api_unchanged.py -q` passed with `33 passed, 1 skipped`.
- [x] Validate the real async smoke snippet in the deployed worker runtime after the build fix. Proof: production async job `f5a3c1beb9714ed0b8073b129026c130` on `https://lean-ui-production.up.railway.app/api/async/check` completed on 2026-03-12 with `results[0].response = {}` for `import Mathlib` / `example : 1 = 1 := by rfl`; gated real-runtime smoke coverage lives in `backend/tests/test_repl_runtime_smoke.py`.
- [x] Redeploy the worker/API and re-run the production async repro for the smoke and Qwen snippets. Proof: `railway deployment list --service lean-ui --environment production` shows deployment `2ca9611d-30ea-4217-a045-122404ec878c | SUCCESS`; production async job `e845badd33084ae68a3885f9a77eb040` completed on 2026-03-12 for the provided Qwen snippet with only tactic warnings and no `Failed to run header on REPL` / `Failed to start REPL`.
